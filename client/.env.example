# Public API base (production: https://api.yourdomain.com)
NEXT_PUBLIC_SERVER_URL=http://localhost:5000

# Optional — only needed for Stripe.js / Elements; Checkout Session redirect uses the backend only.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
